name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Regenerate corpus, DB, and eval sets
        run: PYTHONPATH=. python3 scripts/run_pipeline.py

      - name: Run tests
        run: PYTHONPATH=. python3 -m pytest tests/ -v
